# SQL_Joins_Mini_Project__courses_per_student

# Query: Courses Per Student

## 📌 Problem Statement
Count the number of courses each student is enrolled in, using JOINs between the `students` and `enrollments` tables.

This query helps track course load per student and is useful for advising, academic planning, and capacity management.

## 🧠 SQL Concepts Used
- `JOIN` (students ↔ enrollments)
- `GROUP BY` to count enrollments per student
- `COUNT()` aggregate function

## 🗃 Tables Involved
- `students`: Holds student ID and name
- `enrollments`: Links students to courses
